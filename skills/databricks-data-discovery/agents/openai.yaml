interface:
  display_name: "Databricks Data Discovery"
  short_description: "Discover, explore, and query Databricks data via Genie — the CLI equivalent of the Genie One MCP. MUST be invoked whenever the user asks to find or locate data ('what tables are in X', 'where does..."
  icon_small: "./assets/databricks.svg"
  icon_large: "./assets/databricks.png"
  brand_color: "#FF3621"
  default_prompt: "Use $databricks-data-discovery for discover, explore, and query databricks data via genie — the cli equivalent of the genie one mcp. must be invoked whenever the user asks to find or locate data ('what tables are in x', 'where does."
